#!/usr/bin/env bash
# This script creates an RSA key pair with the specified requirements.

# Define the key file name
key_file="school"

# Generate RSA key pair with 4096 bits and protected by the passphrase "betty"
ssh-keygen -t rsa -b 4096 -C "Generated key for educational purposes" -f "$key_file" -N "betty"

